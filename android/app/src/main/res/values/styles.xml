<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Flutter v2 embedding expects two themes:
      - LaunchTheme: shown while the app is initializing (native splash)
      - NormalTheme: applied after Flutter renders the first frame

      Keeping NormalTheme translucent/transparent causes the OS to show a blank window
      and can prevent flutter_native_splash from appearing.
    -->
    <style name="LaunchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
    <style name="NormalTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
